<div class="bg-image"></div>

<%= render_sir_trevor(@page.body) %>

<div class="container">
    <div id="hero">
      <div class="col-sm-12 text-center">
        <h1 class="pacifico">
          Tour De Fun
        </h1>
        <h2>
          <%= Timeslot.first_show_time %>
        </h2>

        <div id="stats">
          <%= link_to timeslots_path do  %>
            <%= pluralize artists_count, 'artist' %>,
            <%= pluralize venues_count, 'venue' %>,
            and all the bikes&nbsp;&rarr;
          <% end %>
        </div>

      </div>
    </div>
</div>

