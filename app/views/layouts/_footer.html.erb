<div class="container">
  <div class="row">
      <% platinum_sponsors.each do |sponsor| %>
    <div class="col-sm-4">
        <%= link_to_unless sponsor.website.blank?,
              image_tag(sponsor.logo.url(:small), alt: sponsor.name)
          %>
    </div>
      <% end %>
  </div>

  <div class="row">
    <div class="col-sm-12">
      <p>
        &copy; <%= Date.today.year %> Tour de Fun
      </p>
    </div>
  </div>
</div>
