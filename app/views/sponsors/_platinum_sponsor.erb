<div class="col-xs-6 col-sm-4">
  <div class="platinum-sponsor">
    <% if platinum_sponsor.website.blank? %>
      <%= image_tag platinum_sponsor.logo_url(:large),
            class: 'img-responsive' %>

    <% else %>
      <%= link_to platinum_sponsor.website do %>
        <%= image_tag platinum_sponsor.logo_url(:large),
            class: 'img-responsive' %>
      <% end %>
    <% end %>
  </div>
</div>
