<% if @page and not @page.home? %>
  <div id="page-title" class="text-center">
    <h1 class="pacifico">
      <%= @page.title %>
    </h1>
    <h4><%= @page.try(:subtitle) %></h4>
  </div>
<% end %>
