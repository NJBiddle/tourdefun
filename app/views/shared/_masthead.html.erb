<% if @page and not @page.home? %>
  <div id="masthead">
    <div class="container">
      <div class="row">
        <div class="col-sm-12">
          <h1 class="text-center"><%= @page.title %></h1>
        </div>
      </div>
    </div>
  </div>
<% end %>
