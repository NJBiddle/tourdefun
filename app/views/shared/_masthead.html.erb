<% if @page and not @page.home? %>
  <div id="masthead">
    <div class="container">
      <div class="row">
        <div class="col-12-sm">
          <h1><%= @page.title %></h1>
        </div>
      </div>
    </div>
  </div>
<% end %>
